import template from './item-card.html';
import controller from './item-card.controller';
import './item-card.scss'

let itemCardComponent = {
  bindings: {},
  template,
  controller
};

export default itemCardComponent;
