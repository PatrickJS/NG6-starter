import template from './item.html';
import controller from './item.controller';

import './item.scss';

const component = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default component;
